{{#each data}}
<div class="card">
  <h5 class="card-header">{{company}}</h5>
  <div class="card-body">
    <h5 class="card-title">Renews:{{dayofmonth}} || Cost per month: ${{cost}}</h5>
  </div>
</div>
{{/each}}